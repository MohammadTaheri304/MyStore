add refragmentation for dbFileEngine and indexFileEngine.
optimize clusterEngine communication.
use cryptoghraphy for network engine.
make IT transactional!
dynamic config!
add admin panel.
add node monitor.
donnot save other nodes data. just remmeber which node is the owner of a key.
Currently the cluster should be a complete graph. make it work with not complete graph schema.
add data load balancer. distribute data among nodes.
